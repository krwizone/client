services:
  - type: web
    name: knight-multiplayer
    env: node
    plan: free
    autoDeploy: true
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    regions:
      - oregon
